services:
devpulse-api:
image: python:3.11-slim
working_dir: /app
volumes:
- ./:/app
env_file:
- .env
command: bash -lc "pip install -r requirements.txt && uvicorn devpulse_ai.api:app --host ${APP_HOST:-0.0.0.0} --port ${APP_PORT:-8000}"
ports:
- "8000:8000"